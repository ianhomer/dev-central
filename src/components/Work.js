import React from 'react';
import PropTypes from 'prop-types'

const Work = ({ handle }) => {
  return (
  <div>
    work
  </div>
)}

Work.propTypes = {
  handle: PropTypes.object.isRequired,
}

export default Work